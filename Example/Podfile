use_frameworks!

platform :ios, '8.0'

target 'AMKCrashProtector_Example' do
  pod 'AMKCrashProtector', :path => '../'

  target 'AMKCrashProtector_Tests' do
    inherit! :search_paths

    
  end
end
